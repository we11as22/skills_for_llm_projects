# Bootstrap config: skill bank — one run installs only selected skills to selected vibecodes.
# After git clone: edit this file (or leave defaults), then run ./scripts/bootstrap_vibecoding.sh

# ─── What to install ─────────────────────────────────────────────────────────
# Categories = subfolders under skills/ (each category contains skill dirs with SKILL.md).
# Use "all" to install every category.
# NOTE: "common" is always installed regardless of this setting (mandatory guardrail skills).
CATEGORIES="langgraph llm frontend backend platform"

# Optional: extra skills by name (from any category). Space-separated.
# Use when you want specific skills without a whole category.
# SKILLS_EXTRA="langgraph-core-agent-builders react-js-engineer"

# ─── Where to install (vibecodes) ─────────────────────────────────────────────
# Targets: claude, codex, cursor, windsurf, cline, aider, roo, kilocode, opencode. Use "all" for all.
TARGETS="claude codex cursor windsurf cline aider roo kilocode opencode"

# ─── Install location: default = home (~), or set path for project-local install ─
# Empty = use default (~/.cursor/skills, ~/.claude/skills, etc.).
# Set to a directory (e.g. /path/to/myproject) to install into that folder's vibecode dirs
# (e.g. /path/to/myproject/.cursor/skills). One run = only skills, no extra files.
#
# CLAUDE_PATH=
# CODEX_PATH=
# CURSOR_PATH=
# WINDSURF_PATH=
# ROO_PATH=
# KILO_PATH=
# OPENCODE_PATH=

# Local overrides: create bootstrap.config.local (same keys) — it is gitignored.
